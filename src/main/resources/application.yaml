server:
  port: ${PORT:8080}
  shutdown: graceful

spring:
  application:
    name: notes-api

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

logging:
  level:
    root: INFO
